ID: ADM2
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: HDB1
   type: hdb
   path: DB/HDB1
   requires:
    - name: hdi_HDB1

resources:
 - name: hdi_HDB1
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
